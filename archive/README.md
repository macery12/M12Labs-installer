# Archive – Legacy Code

This directory contains archived code that is **no longer part of the active installer**.
It is kept here for reference only and is not used by any current install, update, or build flow.

## Contents

| Directory | Description |
|-----------|-------------|
| `installer/` | Original Textual TUI-based extension installer (superseded by the panel installer in `/installer`). |

> **Do not rely on this code for new work.**  The active panel installer lives under `/installer` at the repository root.
